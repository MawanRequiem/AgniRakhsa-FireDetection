[wokwi]
version = 1
firmware = ".pio/build/esp32-s3-devkitc-1/firmware.bin"
elf = ".pio/build/esp32-s3-devkitc-1/firmware.elf"

[net]
# Forward localhost on the host to Wokwi network
# The MCU can access the backend at http://host.wokwi.internal:8000


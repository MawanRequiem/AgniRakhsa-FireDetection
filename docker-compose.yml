version: "3.8"

services:
  web:
    build: ./web
    ports:
      - "5173:5173"
    env_file:
      - ./web/.env
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env

  whatsapp-gateway:
    build: ./whatsapp-gateway
    ports:
      - "3001:3001"
    env_file:
      - ./whatsapp-gateway/.env
    depends_on:
      - backend

// Placeholder — Baileys message handler
// Will handle incoming WhatsApp messages and forward to backend AI

export function handleMessage(sock, message) {
    // TODO: Implement message handling
    console.log('Received message:', JSON.stringify(message, null, 2))
}
